apiVersion: apps/v1
kind: Deployment
metadata:
  name: tvs-dashboard
  namespace: traffic
spec:
  replicas: 3
  selector:
    matchLabels:
      app: tvs-dashboard
  template:
    metadata:
      labels:
        app: tvs-dashboard
    spec:
      containers:
      - name: dashboard
        image: hamzamalik1/traffic-dashboard:latest
        ports:
        - containerPort: 8501
        env:
        - name: API_URL
          value: "http://tvs-api-svc:80"
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "128Mi"
            cpu: "100m"
        volumeMounts:
        - name: evidence-data
          mountPath: /app/outputs
      volumes:
      - name: evidence-data
        persistentVolumeClaim:
          claimName: evidence-pvc
